version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Build starting..."
    build:
      commands:
        - echo "Build complete"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths:
      - 'node_modules/**/*'
